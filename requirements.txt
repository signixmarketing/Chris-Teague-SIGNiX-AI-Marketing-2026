# Lease origination sample app — vehicle (jet pack) leasing.
# Install in a venv: pip install -r requirements.txt
Django>=5.0,<6
django-jazzmin>=3.0
Pillow>=10.0
pdfkit>=1.0
pypdf>=4.0
requests>=2.28
